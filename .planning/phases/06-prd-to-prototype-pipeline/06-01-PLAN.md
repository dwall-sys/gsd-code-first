---
phase: 06-prd-to-prototype-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/prototype.md
autonomous: true
requirements:
  - PRD-01
  - PRD-02
  - PRD-03
  - PRD-04
  - PRD-07

must_haves:
  truths:
    - "User can run /gsd:prototype and have .planning/PRD.md auto-detected as PRD source"
    - "User can run /gsd:prototype --prd path/to/file.md and have that file used as PRD source"
    - "User is prompted to paste PRD content when no PRD file exists"
    - "User sees a numbered list of extracted acceptance criteria before any code generation begins"
    - "User can confirm or correct the extracted AC list before proceeding"
    - "Each acceptance criterion becomes a @gsd-todo(ref:AC-N) tag instruction in the Task() prompt to gsd-prototyper"
  artifacts:
    - path: "commands/gsd/prototype.md"
      provides: "PRD pipeline command orchestrator with Steps 0-4"
      contains: "--prd"
  key_links:
    - from: "commands/gsd/prototype.md"
      to: "agents/gsd-prototyper.md"
      via: "Task() tool spawn with PRD-enriched context"
      pattern: "gsd-prototyper"
    - from: "commands/gsd/prototype.md"
      to: ".planning/PRD.md"
      via: "auto-detect Read"
      pattern: "PRD\\.md"
---

<objective>
Rewrite `commands/gsd/prototype.md` to add PRD ingestion (Steps 0-4): flag parsing for --prd/--interactive/--non-interactive, three-way PRD resolution (flag, auto-detect, paste), semantic AC extraction, mandatory confirmation gate, and gsd-prototyper Task() spawn with AC-enriched context.

Purpose: Transform /gsd:prototype from a simple agent-spawn into a PRD-driven pipeline entry point. This plan covers everything up to and including the first prototype generation pass.

Output: Rewritten `commands/gsd/prototype.md` with Steps 0-4 of the v1.1 process.
</objective>

<execution_context>
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/workflows/execute-plan.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-prd-to-prototype-pipeline/06-RESEARCH.md

<interfaces>
<!-- Current prototype.md structure that will be rewritten -->
From commands/gsd/prototype.md:
- Frontmatter: name, description, argument-hint, allowed-tools
- Sections: <objective>, <context>, <process>
- Current process: 4 steps (spawn prototyper, wait, extract-tags, show results)
- Uses: Task tool, Bash tool, $ARGUMENTS, @.planning/*.md context refs

From commands/gsd/iterate.md (pattern source):
- Flag check pattern: `Check if \`--non-interactive\` is present in \`$ARGUMENTS\`.`
- ARC executor selection: `ARC_ENABLED=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-get arc.enabled 2>/dev/null || echo "true")`
- Approval gate pattern: AskUserQuestion with yes/no response handling
- extract-tags command: `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" extract-tags --format md --output .planning/prototype/CODE-INVENTORY.md`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite prototype.md with PRD pipeline Steps 0-4</name>
  <files>commands/gsd/prototype.md</files>
  <read_first>
    - commands/gsd/prototype.md (current file to rewrite)
    - commands/gsd/iterate.md (pattern source for flags, approval gates, ARC check)
    - get-shit-done/references/arc-standard.md (tag syntax for @gsd-todo ref:AC-N)
    - .planning/phases/06-prd-to-prototype-pipeline/06-RESEARCH.md (architecture patterns, code examples)
  </read_first>
  <action>
Rewrite `commands/gsd/prototype.md` completely. The new file must contain:

**Frontmatter** — Update to reflect new capabilities:
- `name: gsd:prototype` (unchanged)
- `description:` update to mention PRD ingestion, AC extraction, and autonomous iteration
- `argument-hint: "[path] [--phases N] [--prd <path>] [--interactive] [--non-interactive]"`
- `allowed-tools:` add `AskUserQuestion` to the existing list (Read, Write, Edit, Bash, Task, Glob, Grep)

**objective section** — Rewrite to describe the full PRD-to-prototype pipeline:
- Spawns gsd-prototyper to build prototype from PRD acceptance criteria
- Three-way PRD resolution: --prd flag, auto-detect .planning/PRD.md, paste prompt
- Shows AC confirmation gate before any code generation
- Each AC becomes a @gsd-todo(ref:AC-N) tag in prototype code
- Supports --interactive for step-by-step mode and --non-interactive for CI
- Document all arguments: path, --phases N, --prd path, --interactive, --non-interactive

**context section** — Keep existing refs ($ARGUMENTS, @.planning/PROJECT.md, @.planning/REQUIREMENTS.md, @.planning/ROADMAP.md)

**process section** — Write Steps 0-5 (Plan 02 will add Steps 6-7 for the iteration loop):

**Step 0: Parse flags** (per D-01, D-06, D-10)
- Check $ARGUMENTS for --prd <path>, --interactive, --non-interactive, --phases N
- Follow iterate.md flag-check pattern exactly: prose instructions checking $ARGUMENTS

**Step 1: Resolve PRD content** (per D-01, implements PRD-01, PRD-02, PRD-03)
- Priority chain per D-01:
  1. If --prd <path> is present: Read the file at <path>. If file doesn't exist, STOP with error.
  2. Else check if .planning/PRD.md exists via Bash: `test -f .planning/PRD.md && echo "exists" || echo "missing"`
     If exists: Read .planning/PRD.md
  3. Else: Use AskUserQuestion: "No PRD file found at .planning/PRD.md. Paste your PRD content below (or type 'skip' to run without PRD):"
     If user types 'skip': proceed to Step 4 without PRD context (backward-compatible behavior)
     If user pastes content: use that as prd_content
- Log which resolution path was used: "PRD source: --prd flag", "PRD source: auto-detected .planning/PRD.md", or "PRD source: pasted content"
- All three paths produce the same prd_content for downstream processing

**Step 2: Extract acceptance criteria** (per D-08, D-09, implements PRD-04)
- Instruct the AI (self-instruction in command prose) to extract all acceptance criteria from prd_content semantically
- Use this exact extraction prompt structure from the research:
```
Extract all acceptance criteria, requirements, and success conditions from the following PRD.
Output format — one per line:
AC-1: [description in imperative form]
AC-2: [description in imperative form]
...

Rules:
- Include ACs from prose paragraphs, bullet lists, tables, and user stories
- Normalize user stories to acceptance criteria form ("User can..." → "Users can...")
- If the PRD has explicit numbered/labeled ACs, preserve their intent but renumber sequentially
- If no explicit ACs exist, infer them from goals and scope sections
- Output ONLY the numbered list — no headers, no commentary

PRD content:
[prd_content]
```
- Store the extracted AC list for use in Steps 3 and 4

**Step 3: Confirmation gate** (per D-03, D-04, implements PRD-07)
- Check if --non-interactive is present:
  - If YES: Log "Auto-approving N acceptance criteria (--non-interactive mode)." and proceed to Step 4
  - If NO: Display the numbered AC list to the user, then use AskUserQuestion:
    "Found N acceptance criteria from PRD. Review the list above. Proceed with prototype generation? [yes / provide corrections]"
  - If user says yes/y/approve: proceed to Step 4
  - If user provides corrections: incorporate corrections into the AC list and re-display for confirmation (loop back)
  - IMPORTANT per D-03: There is NO code path that reaches Step 4 without explicit approval or --non-interactive. The confirmation gate is mandatory.

**Step 4: Spawn gsd-prototyper (first pass)** (per D-02, D-09, implements PRD-04)
- Spawn gsd-prototyper agent via Task tool with enriched context:
  - Pass $ARGUMENTS (including --phases if present)
  - Pass the full AC list as structured text in the Task() prompt
  - Include this exact instruction in the Task() prompt:
    "**Acceptance criteria to implement as @gsd-todo tags:**
    [paste AC list here]

    For each acceptance criterion listed above, create exactly one @gsd-todo tag with `ref:AC-N` metadata in the prototype code where N is the criterion number.
    Example: `// @gsd-todo(ref:AC-1) User can run /gsd:prototype with PRD auto-detection`"
  - If no PRD was provided (user typed 'skip' in Step 1): spawn gsd-prototyper with original behavior (just $ARGUMENTS, no AC list)
- Wait for gsd-prototyper to complete

**Step 5: Run extract-tags** (existing step, renumbered)
- Run: `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" extract-tags --format md --output .planning/prototype/CODE-INVENTORY.md`
- If PRD was provided: count AC-linked todos: `grep -c "ref:AC-" .planning/prototype/CODE-INVENTORY.md 2>/dev/null || echo "0"`
- Log: "AC todos remaining: N" (or "No PRD-linked todos to track" if no PRD)

**PLACEHOLDER for Steps 6-7** — Add this comment at the end of the process section:
```
<!-- Steps 6-7 (autonomous iteration loop and final report) will be added by Plan 02 -->
```

Do NOT implement any deferred features: no --init-prd, no remote URLs, no --fix flag.
  </action>
  <verify>
    <automated>grep -c "Step 0\|Step 1\|Step 2\|Step 3\|Step 4\|Step 5" commands/gsd/prototype.md | xargs test 6 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "\-\-prd" commands/gsd/prototype.md` returns 0 (PRD-02 flag documented)
    - `grep -q "PRD.md" commands/gsd/prototype.md` returns 0 (PRD-01 auto-detect path)
    - `grep -q "AskUserQuestion" commands/gsd/prototype.md` returns 0 (PRD-03 paste prompt)
    - `grep -q "ref:AC-" commands/gsd/prototype.md` returns 0 (PRD-04 tag format)
    - `grep -q "confirmation\|Proceed.*prototype\|approve" commands/gsd/prototype.md` returns 0 (PRD-07 confirmation gate)
    - `grep -q "\-\-non-interactive" commands/gsd/prototype.md` returns 0 (CI bypass path)
    - `grep -q "\-\-interactive" commands/gsd/prototype.md` returns 0 (PRD-06 flag present for Plan 02)
    - `grep -q "gsd-prototyper" commands/gsd/prototype.md` returns 0 (Task spawn preserved)
    - `grep -q "extract-tags" commands/gsd/prototype.md` returns 0 (extract-tags step preserved)
    - The frontmatter `allowed-tools` list includes AskUserQuestion
    - The file has no references to --init-prd, Notion, Confluence, or --fix (deferred items)
  </acceptance_criteria>
  <done>
    prototype.md contains Steps 0-5 implementing PRD resolution (3 paths), AC extraction, mandatory confirmation gate, enriched Task() spawn with AC list, and extract-tags. All 5 requirements (PRD-01, PRD-02, PRD-03, PRD-04, PRD-07) are addressed. The file is syntactically valid Markdown with proper frontmatter.
  </done>
</task>

</tasks>

<verification>
- `head -10 commands/gsd/prototype.md` shows valid YAML frontmatter with updated description and argument-hint
- All 6 steps (0-5) present in the process section
- The PRD resolution priority chain follows D-01 exactly: --prd flag > auto-detect > paste
- The confirmation gate is mandatory per D-03 with only --non-interactive as bypass
- The Task() prompt to gsd-prototyper includes AC list and ref:AC-N instruction per D-08/D-09
- No deferred features present (--init-prd, remote URLs, --fix)
</verification>

<success_criteria>
- /gsd:prototype command file contains complete PRD ingestion pipeline (Steps 0-5)
- PRD resolution supports all three input paths (flag, auto-detect, paste)
- AC extraction uses semantic prompt, not structural regex
- Confirmation gate is mandatory with --non-interactive as only bypass
- gsd-prototyper receives AC list with explicit ref:AC-N tag instruction
- extract-tags runs after first pass and counts AC-linked todos
- File is ready for Plan 02 to add iteration loop (Steps 6-7)
</success_criteria>

<output>
After completion, create `.planning/phases/06-prd-to-prototype-pipeline/06-01-SUMMARY.md`
</output>
